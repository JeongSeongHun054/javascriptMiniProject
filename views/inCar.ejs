<!DOCTYPE html>
<html lang="ko">
    <head>
        <meta charset="utf-8" />
        <title>주차장관리시스템</title>
        <meta name="viewport" content="width=device-width" />
        <link rel="stylesheet" href="stylesheets/inCar.css"/>
        <link rel="stylesheet" href="https://use.fontawesome.com/releases/v5.8.1/css/all.css"/>
        <title>입차처리</title>
    </head>
    <body>
        <img src="images/cute.gif" alt="주차">
        <div class="back"></div>
        <div class="header-container">
            <header>
                <a href="/">입차</a>
            </header>
        </div>
        <div class="container">
            <section>
                <div class="info-container">
                    <ul>
                        <li><i class="fas fa-mobile-alt"></i>&nbsp;010-8266-1465</li>
                        <li><i class="far fa-envelope"></i>&nbsp;gnsl1465.naver.com</li>
                    </ul>
                    <form action="/inCar" method="post">
                        <input type="text" id="incar" name="carNumber" placeholder="Insert Car Number" required="차량번호를 입력하세요!"/><br/>
                        <input type="submit" id="open" value="check"/>
                    </form>
                </div>
            </section>
        </div>
        <div class="modal-wrapper" style="display: none">
            <div class="modal">
                <p>잘못된 형식 또는 이미 입차된 차량입니다.<br/>이 창은 3초후에 닫힙니다.</p>
                <div class="close-wrapper">
                    <button id="close">닫기</button>
                </div>
            </div>
        </div>
        <div class="footer-container">
            <footer>
                <p>Hun Parking lot<br/>
                &copy;JeongSeongHun All Rights Reserved</p>
            </footer>
        </div>
        <script>
            const open = document.querySelector('#open');
            const close = document.querySelector('#close');
            const modal = document.querySelector('.modal-wrapper');

            open.addEventListener('click',Modal);

            close.onclick = () => {
                modal.style.display = 'none';
            }

            function Modal(){
                let inputValue = document.querySelector('input').value;
                const rex = /^[0-9]{2,3}[가-힣]{1}[0-9]{4}$|^[가-힣]{2,3}[0-9]{2}[가-힣]{1}[0-9]{4}$/;
                if(rex.test(inputValue) == true){
                    inputValue.value = '';
                    return 0;
                }else if(rex.test(inputValue) == false){
                    modal.style.display = 'flex';
                }
            }


        </script>
    </body>
</html>